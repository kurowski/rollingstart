#!/usr/bin/env python3
# rolling-guard: the write-mode scope guard, run by hooks.json on
# PreToolUse for Edit, Write, MultiEdit, and NotebookEdit. See
# lib/rolling/guard.py.
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), "..", "lib"))
from rolling.guard import main  # noqa: E402

sys.exit(main(sys.argv[1:]))
