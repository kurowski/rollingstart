#!/usr/bin/env python3
# rolling-check-reply: the citation check on the tutor's reply, run by
# hooks.json on Stop. See lib/rolling/reply.py.
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), "..", "lib"))
from rolling.reply import main  # noqa: E402

sys.exit(main(sys.argv[1:]))
