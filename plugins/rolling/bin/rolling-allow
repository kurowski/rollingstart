#!/usr/bin/env python3
# rolling-allow: the toolkit allows its own commands, run by hooks.json
# on PreToolUse for Bash and Skill. See lib/rolling/allow.py.
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), "..", "lib"))
from rolling.allow import main  # noqa: E402

sys.exit(main(sys.argv[1:]))
